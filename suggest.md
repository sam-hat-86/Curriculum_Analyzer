# Curriculum Analyzer 実装開発側からの改善提案書 (suggest.md)

本書は、システム実装・動作検証・UI設計を担当した開発エンジニア側の視点から、今後の運用安定性、現場での利便性、保守・改修の持続可能性をさらに高めるための技術的・実務的な改善アイデアをまとめたものです。
計画立案・分析を担当されるAIおよびご担当者様にて、今後のロードマップ策定や要件定義の検討材料としてご活用ください。

---

## 目次

1. [DOM抽出・ブラウザ連携の堅牢化（Web Scraping）](#1-dom抽出ブラウザ連携の堅牢化web-scraping)
2. [解析・判定エンジンの保守性と柔軟性（Parser & Rules）](#2-解析判定エンジンの保守性と柔軟性parser--rules)
3. [UI・実務オペレーションの効率化（User Experience）](#3-ui実務オペレーションの効率化user-experience)
4. [Excel帳票・データ活用の高度化（Reporting & Actionability）](#4-excel帳票データ活用の高度化reporting--actionability)
5. [品質保証・DevOps・配布形式（Engineering & Maintenance）](#5-品質保証devops配布形式engineering--maintenance)
6. [推奨する優先度マトリクス（まとめ）](#6-推奨する優先度マトリクスまとめ)

---

## 1. DOM抽出・ブラウザ連携の堅牢化（Web Scraping）

### 1.1 セレクタ・XPathの設定外出し（No-Buildチューニング）
- **現状の課題**:
  今回、教室名取得のために絶対XPath（`/html/body/div/.../span/span`）を追加しました。しかし、対象Webシステム側でバナー追加、ラッパーDIVの変更、クラス名刷新などの軽微なデザイン改修が入ると、絶対XPathは即座に無効化（破損）するリスクがあります。
- **改善提案**:
  - `config.ini` または `selectors.json` に、教室名・年度・テーブル等のXPath/CSSセレクタを外出しし、アプリを再ビルド（再配布）することなく設定値の書き換えだけで追従可能にする。
  - 複数候補（フォールバックリスト）を配列で持たせ、上から順にマッチしたものを採用する設計にする。

### 1.2 非同期通信（SPA）描画完了の検知強化
- **現状の課題**:
  教室選択や条件変更の直後、DOMがAjax/Fetchで非同期再描画される途中でユーザーが `F9` を押すと、前画面のテーブルを読み取ってしまうか、不完全な行数で蓄積される恐れがあります。
- **改善提案**:
  - `extract_table.js` 内でローディングスピナー（`.loading`, `.spinner`）の非表示確認や、`MutationObserver` を活用した「DOMの安定（直近500ms変化なし）」を判定条件に組み込む。
  - レコード読み取り前に「データ読み込み中」である場合は、トースト通知で「描画完了をお待ちください」と案内し、誤読み取りを未然に防止する。

### 1.3 ページネーションの自動連続取得（オートパイロット）
- **現状の課題**:
  指示書が5ページ、10ページとある場合、ユーザーが手動で「次へ」→「F9」を繰り返す必要があり、読み飛ばしや二重読み取りのヒューマンエラーが発生しやすい。
- **改善提案**:
  - ツールバーに「全ページ一括読み取り」オプションを追加。
  - 「現在のページを読み取り」→「次へリンクを自動クリック」→「画面遷移検知」→「次ページ読み取り」を最終ページまで自動ループするモードを実装。

---

## 2. 解析・判定エンジンの保守性と柔軟性（Parser & Rules）

### 2.1 既知見出し・教材名辞書の外部JSON化
- **現状の課題**:
  講師ごとに新しい記号（例: `■今日の指導`, `▼次回復習`, `【家庭学習】`）や独自の教材略称（`フォレスタ`、`新演習`、`WinPass` 等）が使われるたびに、Pythonコード（`constants.py` / `normalization.py`）を修正してビルドし直す必要があります。
- **改善提案**:
  - `user_rules.json`（または `custom_dictionary.json`）を設け、以下の項目をユーザー/管理者側で追記可能にする：
    1. **見出し同義語マップ**（例: `{"授業内容": ["当日の指導", "本日の進捗"]}`）
    2. **主要教材リスト**（部分一致で教材と判定するキーワード一覧）
    3. **除外ワードリスト**（未分類テキストから除外する定型文など）
  - v7で実装された「未知見出し収集機能（`unknown_headings.json`）」と連動させ、GUI上で「この未知見出しを正式な【宿題】として辞書登録する」ボタンを設けることで、運用しながら辞書が自己成長するサイクルを構築。

### 2.2 判定根拠（Reasoning Trace）の可視化
- **現状の課題**:
  Excel上で `ERROR` や `REVIEW` と出た際、「なぜERRORになったのか（どの単語が見つからなかったのか、どの条件で弾かれたのか）」が直感的に分からないと、現場の講師・管理者が納得しづらい。
- **改善提案**:
  - 判定結果モデル（`EvaluationResult`）に「判定ログ（トレース情報）」を持たせ、Excelの備考欄またはコメント（ホバー表示）に以下のように記載：
    > 例: `[判定根拠] 教材名「新中学問題集」を検出済 / 所持状況「持参」あり / ページ数・進捗の具体的数値なし -> 内容不足(ERROR)`

---

## 3. UI・実務オペレーションの効率化（User Experience）

### 3.1 集計前の「プレビュー・クイック修正」ドロワー/画面
- **現状の課題**:
  現状は「読み取り（F9）」→即「Excel出力」の流れです。途中で「1件だけ誤読した」「判定がREVIEWになった行の内容をその場で確認したい」場合でも、一度Excelを開いて確認・修正するしかありません。
- **改善提案**:
  - メイン画面の右側または別タブに、現在蓄積中のレコードを簡易テーブル（生徒名・判定バッジ・修正項目）でリアルタイム表示するプレビューペインを設ける。
  - レベル別（CRITICAL/ERROR/REVIEW/WARNING/PASS）でフィルタリングでき、Excel出力前に「現場での事前確認」を完結可能にする。

### 3.2 教室切り替え時の「未出力データ保持アラート」
- **現状の課題**:
  別教室のページに移動してそのまま読み取ると、異なる教室のデータが同一セッションに混在し、Excel集計が意図せず合算される可能性があります。
- **改善提案**:
  - 既にデータが蓄積されている状態で、異なる教室コード/教室名のページが読み取られた場合：
    > 「現在の蓄積データは【A教室】です。【B教室】のデータが検出されました。現在のデータをExcel出力して新規セッションを開始しますか？」
    という確認ダイアログを表示して誤混在を防ぐ。

---

## 4. Excel帳票・データ活用の高度化（Reporting & Actionability）

### 4.1 Excelファイルオープン時の競合・ロック対策（自動リネーム）
- **現状の課題**:
  出力先と同名のExcelファイル（`授業指示書_精査結果.xlsx`）をユーザーが既にExcelソフトで開いている状態で再度「Excel出力」を押すと、Windowsのファイルロック（PermissionError）で出力が失敗します。
- **改善提案**:
  - ファイルがロックされている場合、エラーで中断せず自動的にタイムスタンプを付与（例: `授業指示書_精査結果_153022.xlsx`）して保存し、「既存ファイルが開かれていたため別名で保存しました」と通知する。

### 4.2 「要対応タスク一覧（Action Items）」専用シートの追加
- **現状の課題**:
  全体シート（数千行）から、修正が必要な行を人間がオートフィルタで絞り込む手間が発生している。
- **改善提案**:
  - Excelブックの第1シート（または第2シート）に、**`CRITICAL` および `ERROR` の行だけを自動抽出した【要修正タスク一覧】** シートを設ける。
  - 講師名ごとにグループ化（または講師別シート化）し、教室長がそのまま該当講師へ「ここを修正してください」と転送・指示出しできる形式にする。

### 4.3 教室別・全体のエラー率ダッシュボード（グラフ化）
- **改善提案**:
  - 「集計サマリー」シートに、openpyxlを用いた「判定レベル別比率（円グラフ/棒グラフ）」や「項目別エラー発生頻度（宿題の具体性不足、教材所持抜け等）」のチャートを自動生成。
  - 月次・週次でのクオリティ推移を視覚的に把握できるようにする。

---

## 5. 品質保証・DevOps・配布形式（Engineering & Maintenance）

### 5.1 DOM抽出（`extract_table.js`）のモックHTML自動テスト
- **現状の課題**:
  現在のpytest（`test_regression_sample.py`）は、パーサーと判定エンジンのPythonロジックを100%テストしていますが、ブラウザのDOM抽出（JavaScriptとHTMLの相互作用）は自動テストに含まれていません。
- **改善提案**:
  - 実際の指示書WebページのHTML（個人情報をマスクしたもの）を `tests/fixtures/sample_page.html` として保存。
  - Pythonの `playwright` または `selenium`（またはローカルHTTPサーバー＋PyQt6-WebEngineのヘッドレステスト）で `extract_table.js` を流し込み、テーブル・教室名・ページネーションが期待通り抽出されるかをCI/テストスイートで常時検証できるようにする。

### 5.2 Nuitkaビルドと配布形式の最適化
- **現状の課題**:
  `PyQt6-WebEngine` はChromiumエンジンを含むため、Nuitka Onefile形式にすると配布EXEサイズが約150MB〜250MBになり、初回起動時の一時ディレクトリ展開に数秒〜十数秒の待ち時間が発生します。
- **改善提案**:
  - **社内PC固定運用の場合**: 一時展開オーバーヘッドのない「Onefolder形式（ZIP配布）」を検討。起動が爆速（1秒未満）になります。
  - **初配布・一般配布の場合**: Inno Setup等のインストーラー形式を採用し、`Program Files` 等に展開してショートカットを作成する形態にすると、Onefileの解凍遅延とTEMPフォルダ逼迫を解消できます。

---

## 6. 推奨する優先度マトリクス（まとめ）

| 分類 | 提案項目 | 期待効果 | 実装コスト | 推奨優先度 |
|---|---|---|:---:|:---:|
| **堅牢性** | **セレクタ・XPathの設定外出し (`config.ini`)** | Web側のHTML変更にコード改修なしで即応可能 | 低 | ★★★ (高) |
| **実務** | **Excelオープン時の自動リネーム保存** | ファイルロックによる保存失敗ストレスの解消 | 低 | ★★★ (高) |
| **品質** | **`extract_table.js` のHTMLモック自動テスト** | DOM抽出のデグレ・破壊を事前検知 | 中 | ★★★ (高) |
| **実務** | **「要修正タスク一覧」専用シートの自動生成** | 教室長・管理者の指示出し工数を大幅削減 | 低 | ★★☆ (中) |
| **保守性** | **辞書・表記揺れの外部JSON化 (`user_rules.json`)** | 現場特有の略称・新教材に柔軟追従 | 中 | ★★☆ (中) |
| **UI** | **取り込み前プレビューペイン / 教室誤混在防止** | 誤読み取りの現場リカバリ、安心感向上 | 中 | ★★☆ (中) |
| **高度化** | **全ページ自動連続巡回（オートパイロット）** | 複数ページ読み取りのクリック作業の全自動化 | 高 | ★☆☆ (将来) |

---
*作成日: 2026-09-14*  
*作成者: Curriculum Analyzer 実装担当AI*
